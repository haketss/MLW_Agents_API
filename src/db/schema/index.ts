import { rooms } from "../schema/rooms.ts";
import { audioChuncks } from "./audio-chunks.ts";
import { questions } from "./questions.ts";

export const schema = {
    rooms,
    questions,
    audioChuncks
}
